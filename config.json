{
    "Branch"    : "x86_64-via-linux-gnu",
    "Identity"  : "test user \u003ctest@test.com\u003e",
    "Arch"      : "x86_64",
    "OS"        : "linux",
    "Root"      : "/",
    "PlansRepo" : "http://github.com/mrosset/plans",
    "Threads"   : 8,
    "IpfsApi"   : "localhost:5001",
    "Cache"     : "$HOME/.cache/via",
    "DB"        : "/home/mrosset/via/db",
    "Plans"     : "$HOME/src/via/plans",
    "Repo"      : "$HOME/src/via/publish",
    "Binary"    : "http://localhost:8080/ipfs/",
    "Prefix"    : "/home/mrosset/via",
    "Flags"     : [
                "--build=x86_64-via-linux-gnu",
                "--config-cache",
                "--disable-multilib",
                "--disable-nls",
                "--enable-shared",
                "--host=x86_64-via-linux-gnu",
                "--libexec={{.Prefix}}/lib",
                "--prefix=$PREFIX",
                "--sbindir={{.Prefix}}/bin",
                "--sysconfdir={{.Prefix}}/etc",
                "-q"
    ],
    "Env" : {
          "CFLAGS"          : "-pipe -O2 -mtune=generic",
          "CONFIG_SITE"     : "",
          "LDFLAGS"         : "-Wl,-rpath -Wl,$PREFIX/lib -Wl,--as-needed -Wl,--dynamic-linker,$PREFIX/lib/ld-linux-x86-64.so.2",
          "LIBRARY_PATH"    : "$PREFIX/lib",
          "MAKEFLAGS"       : "-sw -j8",
          "PATH"            : "$GOPATH/bin:$PREFIX/lib/ccache:$HOME/bin:$PREFIX/bin",
          "PKG_CONFIG_PATH" : "$PREFIX/lib/pkgconfig",
          "PREFIX"          : "{{.Prefix}}"
    },
    "Remove" : [
             "$PREFIX/share/doc",
             "$PREFIX/share/info",
             "$PREFIX/share/man"
    ],
    "PostInstall" : null
}
