{
    "Branch"    : "aarch64-via-linux-gnu",
    "Identity"  : "test user \u003ctest@test.com\u003e",
    "Arch"      : "aarch64",
    "OS"        : "linux",
    "Root"      : "/",
    "PlansRepo" : "http://github.com/mrosset/plans",
    "Threads"   : 4,
    "IpfsAPI"   : "localhost:5001",
    "Cache"     : "$HOME/.cache/via",
    "DB"        : "/home/mrosset/via/db",
    "Plans"     : "$GOPATH/src/github.com/mrosset/via/plans",
    "Repo"      : "$GOPATH/src/github.com/mrosset/via/publish",
    "Binary"    : "http://localhost:8080/ipfs/",
    "Prefix"    : "/home/mrosset/via",
    "Flags"     : [
                "--build={{.Arch}}-via-linux-gnu",
                "--config-cache",
                "--disable-multilib",
                "--disable-nls",
                "--enable-shared",
                "--host={{.Arch}}-via-linux-gnu",
                "--libexec={{.Prefix}}/lib",
                "--prefix=$PREFIX",
                "--sbindir={{.Prefix}}/bin",
                "--sysconfdir={{.Prefix}}/etc",
                "-q"
    ],
    "Env" : {
          "CFLAGS"          : "-pipe -O2",
          "CONFIG_SITE"     : "",
          "LDFLAGS"         : "-Wl,-rpath -Wl,$PREFIX/lib -Wl,--as-needed -Wl,--dynamic-linker,$PREFIX/lib/ld-linux-aarch64.so.1",
          "LIBRARY_PATH"    : "$PREFIX/lib",
          "MAKEFLAGS"       : "-sw -j8",
          "PATH"            : "$GOPATH/bin:$PREFIX/lib/ccache:$PREFIX/bin",
          "PKG_CONFIG_PATH" : "$PREFIX/lib/pkgconfig",
          "PREFIX"          : "{{.Prefix}}"
    },
    "Remove" : [
             "$PREFIX/share/doc",
             "$PREFIX/share/info",
             "$PREFIX/share/man"
    ],
    "PostInstall" : null
}
